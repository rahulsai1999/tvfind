var extratorrent = require('./extratorrent');

extratorrent
  .search('Suicide Squad') // Promise style
  .then(function(torrents) {
	  console.log(torrents);
	    })
  .catch(function(error) {
	      console.error(error);
	    });
